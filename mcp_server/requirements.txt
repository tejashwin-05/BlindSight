# ─── EcoSight MCP Server ─────────────────────────────────────────
# Core MCP framework
mcp>=1.0.0

# HTTP client for external APIs
httpx>=0.27.0

# SSE transport (optional — only needed for --sse mode)
uvicorn>=0.30.0
starlette>=0.38.0

# Utilities
python-dotenv>=1.0.0
